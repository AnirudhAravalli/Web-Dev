<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Weather App</title>
    <link rel="stylesheet" href="/styles/styles.css">
</head>
<body>

    <%- include(`${partialsPath}/header.ejs`) %> 
    <div class="main-content">
        <article class="widget">
            <form class="weatherLocation">
                <input type="text" class="input-field" placeholder="Enter your city">
                <button class="search-button">Search</button>
            </form>
            <div class="icon-and-weatherInfo">
                <div class="weatherIcon">
                    <i></i>               <!-- Responsible for loading up the icon which shows the current weather condition which is loaded dynamically with the javascript file  -->
                </div>
                <div class="weatherInfo">
                    <div class="temperature"><span></span></div>
                    <div class="weatherCondition"></div>
                </div>
            </div>
            <div class="place"></div>
            <div class="date"></div>
        </article>
    </div>

    <script src="js/index.js"></script>
</body>
</html>